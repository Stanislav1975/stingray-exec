#!/usr/bin/env stingray-exec
# vim:filetype=ruby

ssl_verify_mode :none

vs_names = virtual_server.get_virtual_server_names[:names][:item]

# The returned "item" could be nil, a string, or an array of strings
[*(vs_names || [])].each do |n|
  puts n
end
